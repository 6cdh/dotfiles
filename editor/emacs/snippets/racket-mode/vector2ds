# -*- mode: snippet -*-
# name: vector2ds
# key: vector2ds
# --
(define (make-vector2d m n)
   (build-vector m (lambda _ (make-vector n -1))))

(define (vector2d-ref vec i j)
  (vector-ref (vector-ref vec i) j))

(define (vector2d-set! vec i j v)
  (vector-set! (vector-ref vec i) j v))
